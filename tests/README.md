# tests/ — proving it works · owner: **03 · Security** (+ anyone)

What goes here: test matrices and the scripts that run them.

The rule that makes a test worth anything: **run it against the API, not
through our own screens.** Passing through the UI only proves our JavaScript
chose not to draw something. A real check goes to the database directly.
