# KuwaitSat-1 Mission Hub - environment template
# NAMES ONLY. Never put a value in this file.

# Public by design: these two ship in js/config.js and are visible in
# View Source. They are safe ONLY because row level security is on.
SUPABASE_URL=
SUPABASE_PUBLISHABLE_KEY=

# NOT HERE, AND NEVER HERE:
#   Supabase secret / service-role key  -> the n8n credential store
#   Anthropic / model API key           -> the n8n credential store
#   n8n webhook URL and auth header     -> n8n only
#   any account password                -> a password manager
